(bind dist (func ({ ax ay } { bx by }) (do
    (bind { dx dy } { (- bx ax) (- by ay) })
	(sqrt (+ (* dx dx) (* dy dy)))
)))

(bind cmp-pts (func (px py) (do
    (bind dx (dist { 0 0 } px))
    (bind dy (dist { 0 0 } py))
    (< dx dy)
)))

(bind pts [ { 3 3 } { 1 1 } { 2 2 } ])
